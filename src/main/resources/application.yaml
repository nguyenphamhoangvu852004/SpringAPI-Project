server:
  port: 8080

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/products_shop"
    username: root
    password: nguyenvu
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  main:
    banner-mode: "off"

logging:
  level:
    root: warn  
  
  